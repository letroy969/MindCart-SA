import React from "react";

const Footer = () => (
  <footer className="bg-blue-900 text-white p-4 text-center">
    &copy; {new Date().getFullYear()} MindCart SA
  </footer>
);

export default Footer;
